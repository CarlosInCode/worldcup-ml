# Dependencias de RUNTIME del dashboard (Streamlit Cloud / Hugging Face Spaces).
# Rangos amplios (no versiones exactas) para que haya wheels en el Python del host
# (Streamlit Cloud suele usar 3.11–3.13, no 3.14). La carga de modelos es tolerante a
# fallos (ver predict.py), así que pequeñas diferencias de versión no tumban la app.
streamlit>=1.30
plotly>=5.18
polars>=1.0
duckdb>=1.0
pyarrow>=14.0
scikit-learn>=1.3
xgboost-cpu>=2.0; sys_platform == 'linux'
xgboost>=2.0; sys_platform != 'linux'
statsmodels>=0.14
joblib>=1.3
scipy>=1.11
numpy>=1.26,<3
pydantic>=2.5
pydantic-settings>=2.1
